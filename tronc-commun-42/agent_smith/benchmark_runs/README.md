# Benchmark Run Artifacts

Place real SWE-bench benchmark `solution.json`, `task.json`, stdout, and stderr files here after running the 5-model x 3-task comparison.

This directory is intentionally kept free of fabricated JSON artifacts.

